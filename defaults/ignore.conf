# Extended regexes matched against commit subjects; matching commits are
# skipped by lint-range and ignored by the version fold / changelog.
# Append-only: override files can add, not remove, entries.
ignore_re=^Merge
ignore_re=^chore\(release\):
ignore_re=^Revert
