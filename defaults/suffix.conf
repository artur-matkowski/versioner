# Branches (space separated) whose commits print the bare X.Y.Z version.
production_branches=release/production
# Prefix stripped from branch names before sanitizing the suffix.
strip_prefix=release/
# Commit hash length in the suffix.
hash_len=7
